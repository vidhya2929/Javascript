// no code before this below code
'use strict';
// we can activate strict mode only for a specific block or spccififc function
// Always put strict mode in the beginning of the scripts
// Strict mode helps to avoid accidental errors
// 1) forbids us to do something 
// 2)creates visible errors

let hasDriversLicence = false;
const passTest = true;

if(passTest) hasDriverLicence = true;
if(hasDriversLicence) console.log("I can drive :D");
// introduce short list of variable names that are reserved for features
const interface = 'Audio';
const private = 534;